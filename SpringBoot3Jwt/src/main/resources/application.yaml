server:
  port: 8989

#Database Setup - PostgreSQL
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwtdemodb1?currentSchema=jwtschema1
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

